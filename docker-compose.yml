version: '3.8'

services:
  x-reader:
    build: .
    ports:
      - "8000:8000"
      - "5173:5173"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app/backend
    restart: unless-stopped
